# Frontend build (Vite) — read at dev/build time
KOMGA_BASE_URL=https://komga.example.com
KOMGA_API_KEY=replace-me

# Go server (production image / `go run ./server`) — the instance password.
# Server-side only; it never reaches the browser bundle.
APP_PASSWORD=replace-me
